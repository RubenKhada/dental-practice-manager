# Configuración del entorno.
# Copia este archivo a ".env" y ajusta los valores si es necesario.
# El sistema funciona sin cambios: solo se usa PORT para el servidor web.

# Puerto en el que corre el servidor (http://localhost:3000)
PORT=3000
